apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: todolist-role
  namespace: todolist-grupo-02
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list"]
- apiGroups: [""]
  resources: ["pods/log"]
  verbs: ["get"]
